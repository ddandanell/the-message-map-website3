# Server Configuration
# Port for the application (default: 6200)
PORT=6200

# Database Configuration (optional for development with in-memory storage)
# Required only if using PostgreSQL with drizzle-kit
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
